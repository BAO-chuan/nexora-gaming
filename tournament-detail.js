(()=>{'use strict';const db=window.NEXORA_DB;if(!db)return;const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=x=>x?new Intl.DateTimeFormat('vi-VN',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Ho_Chi_Minh'}).format(new Date(x)):'—';const id=new URLSearchParams(location.search).get('id');async function rpc(fn,args={}){const r=await db.rpc(fn,args);if(r.error)throw r.error;return r.data}function bracket(ms){if(!ms?.length)return '<div class="hub-empty">Bracket chưa được Admin tạo.</div>';const rounds=[...new Set(ms.map(x=>x.round_no))];return `<div class="tc-bracket">${rounds.map(r=>`<div class="tc-round"><b>VÒNG ${r}</b>${ms.filter(x=>x.round_no===r).map(m=>`<div class="tc-match"><small>TRẬN ${m.match_no}</small><div class="tc-player ${m.winner_id===m.player1_id?'win':''}">${esc(m.player1_name||'Chờ xác định')} ${m.winner_id===m.player1_id?'🏆':''}</div><div class="tc-player ${m.winner_id===m.player2_id?'win':''}">${esc(m.player2_name||'Chờ xác định')} ${m.winner_id===m.player2_id?'🏆':''}</div></div>`).join('')}</div>`).join('')}</div>`}async function load(){const box=$('tcDetail');if(!id){box.textContent='Thiếu mã giải đấu.';return}try{const t=await rpc('nexora_tournament_detail_v280',{p_tournament_id:id});const p=t.participants||[],pct=Math.min(100,Math.round(+t.registered/Math.max(1,+t.max_players)*100));box.innerHTML=`${t.banner_url?`<img class="tc-banner" src="${esc(t.banner_url)}" alt="">`:''}<div class="tc-meta"><span class="tc-pill">${esc(String(t.status).toUpperCase())}</span><span class="tc-pill">${esc(String(t.format).toUpperCase())}</span><b>${+t.registered}/${+t.max_players}</b></div><h1>${esc(t.title)}</h1><p>${esc(t.description||'')}</p><div class="tc-progress"><i style="width:${pct}%"></i></div><p><b>🎁 Phần thưởng:</b> ${esc(t.reward_text||'Theo thông báo Admin')}</p><p><b>🗓️ Đăng ký đến:</b> ${fmt(t.registration_ends_at)}<br><b>🎮 Bắt đầu:</b> ${fmt(t.starts_at)}${t.ends_at?`<br><b>🏁 Kết thúc dự kiến:</b> ${fmt(t.ends_at)}`:''}</p><h3>📜 Thể lệ</h3><p style="white-space:pre-wrap">${esc(t.rules||'Theo thông báo của Admin.')}</p>${t.result_text?`<h3>🏆 Kết quả</h3><p>${esc(t.result_text)}</p>`:''}<h2>⚔️ Bracket</h2>${bracket(t.matches||[])}<h2>🥇 Bảng xếp hạng / Người tham gia</h2><div>${p.map((x,i)=>`<div class="tc-standing"><span>${x.placement?`<b>TOP ${x.placement}</b> • `:`#${i+1} • `}${esc(x.display_name)}</span><span>${esc(x.status)}</span></div>`).join('')||'<div class="hub-empty">Chưa có người tham gia.</div>'}</div>`}catch(e){box.innerHTML=`<div class="hub-empty">${esc(e.message)}</div>`}}load()})();
