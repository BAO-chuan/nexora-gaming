NEXORA GAMING v2.12.1 — LANDING / SUPABASE HOTFIX

1) Upload/ghi đè index.html lên GitHub.
2) Upload mới landing-footer.css cùng thư mục với styles.css.
3) KHÔNG ghi đè styles.css.
4) KHÔNG sửa app.js, config.js, Clan, Tournament, Reward, Profile.
5) KHÔNG cần chạy SQL.
6) Sau deploy, mở tab riêng tư hoặc hard refresh.

Hotfix:
- index.html dùng đúng cấu trúc auth hiện tại: loginTab/registerTab, loginForm/registerForm.
- Load đúng thứ tự: Supabase SDK -> config.js -> app.js.
- Đổi cache query sang v=2121 để tránh Safari/GitHub Pages giữ JS cũ.
- Giữ Landing/CTA/Footer v2.12 theo theme Nexora.
